import {
    StyleSheet
} from "react-native";

export const styles_main = StyleSheet.create({
    container: {
        width: "100%",
        alignItems: "center",
        flexDirection: "row",
        justifyContent: "space-between",
        height: 80
    },
    detailContainer: {
        alignItems: "center",
        justifyContent: "center"
    },
    subContainer: {
        width: 30,
        height: 30
    }
});