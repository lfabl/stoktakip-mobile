import {
    StyleSheet
} from 'react-native';
export const style_main = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "row",
        paddingVertical: 8
    },
    title: {
        textAlign: "center"
    }
});