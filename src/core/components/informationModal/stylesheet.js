import {
    StyleSheet
} from "react-native";

export const styles_main = StyleSheet.create({
    bottomContainer: {
        justifyContent: "flex-end",
        alignItems: "flex-end",
        flexDirection: "row",
        width: "100%",
    },
});
