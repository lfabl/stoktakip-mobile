import {
    StyleSheet
} from "react-native";

export const styles_main = StyleSheet.create({
    container: {
        width: "100%",
        alignItems: "center",
        flexDirection: "row",
        justifyContent: "space-between"
    },
    subContainer: {
        width: 30,
        height: 30
    }
});